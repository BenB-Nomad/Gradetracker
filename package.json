{
  "name": "gradetracker-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "vercel-build": "cd web && npm run build",
    "build": "cd web && npm run build",
    "dev": "cd web && npm run dev",
    "start": "cd web && npm run start",
    "install": "cd web && npm ci || true"
  },
  "devDependencies": {
    "next": "15.5.2"
  }
}


